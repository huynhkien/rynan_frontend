import { HomeView } from "@/widgets";

export const metadata = {
    title: "Trang chủ",
};

const Main = () => {
    return (
        <>
            <HomeView/>
        </>
    )
}
export default Main;